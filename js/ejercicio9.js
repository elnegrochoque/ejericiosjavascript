let str1 = prompt("ingrese una frase o palabra");
let cont1=0;
while (str1.length>cont1) {
    if ("a"==str1.charAt(cont1)) {
        document.write(str1.charAt(cont1));
    }    if ("e"==str1.charAt(cont1)) {
        document.write(str1.charAt(cont1));
    }    if ("i"==str1.charAt(cont1)) {
        document.write(str1.charAt(cont1));
    }    if ("o"==str1.charAt(cont1)) {
        document.write(str1.charAt(cont1));
    }    if ("u"==str1.charAt(cont1)) {
        document.write(str1.charAt(cont1));
    }
    cont1++;
}